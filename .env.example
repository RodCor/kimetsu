# Kimetsu reads secrets from the process environment first, then this
# workspace .env file. Copy this file to .env and fill in your own token.

# Required for `kimetsu chat` when using the Claude Code provider.
CLAUDE_CODE_OAUTH_TOKEN=

# Optional. Required only for `/image` when the selected model supports
# OpenAI image generation.
OPENAI_API_KEY=
